import { Typography } from "@mui/material"

export const ItemTalle = ({talle}) => {
    return (
        <Typography variant="caption" color="primary.dark">
            Talle: {talle}
        </Typography>
    )
}
